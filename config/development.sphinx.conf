indexer
{
}

searchd
{
  listen = 127.0.0.1:9306:mysql41
  log = /Users/annawojcieszek/Projekty/cuckoo/log/development.searchd.log
  query_log = /Users/annawojcieszek/Projekty/cuckoo/log/development.searchd.query.log
  pid_file = /Users/annawojcieszek/Projekty/cuckoo/log/development.sphinx.pid
  workers = threads
  binlog_path = /Users/annawojcieszek/Projekty/cuckoo/tmp/binlog/development
}

source product_core_0
{
  type = mysql
  sql_host = localhost
  sql_user = root
  sql_pass = 
  sql_db = sklep_development
  sql_sock = /tmp/mysql.sock
  sql_query_pre = SET TIME_ZONE = '+0:00'
  sql_query_pre = SET NAMES utf8
  sql_query = SELECT SQL_NO_CACHE `products`.`id` * 1 + 0 AS `id`, `products`.`name` AS `name`, `products`.`description` AS `description`, `products`.`id` AS `sphinx_internal_id`, 'Product' AS `sphinx_internal_class`, 0 AS `sphinx_deleted`, `products`.`id` AS `id`, `products`.`fabric` AS `fabric`, `products`.`color` AS `color`, `products`.`size_id` AS `size_id`, `products`.`type_id` AS `type_id` FROM `products`  WHERE (`products`.`id` BETWEEN $start AND $end) GROUP BY `products`.`id`, `products`.`name`, `products`.`description`, `products`.`id`, `products`.`id`, `products`.`fabric`, `products`.`color`, `products`.`size_id`, `products`.`type_id`  ORDER BY NULL
  sql_query_range = SELECT IFNULL(MIN(`products`.`id`), 1), IFNULL(MAX(`products`.`id`), 1) FROM `products`
  sql_attr_uint = sphinx_internal_id
  sql_attr_uint = sphinx_deleted
  sql_attr_uint = id
  sql_attr_uint = size_id
  sql_attr_uint = type_id
  sql_attr_string = sphinx_internal_class
  sql_attr_string = fabric
  sql_attr_string = color
  sql_query_info = SELECT `products`.* FROM `products`  WHERE (`products`.`id` = ($id - 0) / 1)
}

index product_core
{
  type = plain
  path = /Users/annawojcieszek/Projekty/cuckoo/db/sphinx/development/product_core
  docinfo = extern
  charset_type = utf-8
  source = product_core_0
}
