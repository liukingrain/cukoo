%tr
  %td.picture= image_tag item.product_picture
  %td= item.product_name + " (rozmiar #{item.product_size})"
  %td.price
    = item.quantity
    = "x"
    = format_price item.product_price
    = "="
    = format_price item.amount_due