= simple_form_for [:admin, @product_size], class: "form-horizontal" do |f|
  .form-group
    = f.input :kind, input_html: { class: "form-control" }
    .actions
      = f.button :submit, class: "btn btn-primary"